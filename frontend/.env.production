# Production build points at the hosted Cloud Run backend (Google Cloud).
# (Firebase VITE_FIREBASE_* values are inherited from .env at build time.)
VITE_API_URL=https://specterops-338535293147.us-central1.run.app
